/* created new class for Bullets to achieve cleaner workflow in index */
// eslint-disable-next-line no-unused-vars
class Bullet {
  constructor(posX, posY, id) {
    this.posX = posX;
    this.posY = posY;
    this.id = id;
  }
}
